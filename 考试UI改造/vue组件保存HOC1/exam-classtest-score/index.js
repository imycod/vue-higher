import hoc from './hoc.js'
import baseView from './base-view.vue';
import config from "./config.js"

/**
 * 高阶组件和配置化魅力是在于，
 * 一个是在一个文件里改后，全局响应，
 * 另一个是组件抽象能力，越抽象的组件，越纯粹，可以是数据业务和视图彻底分离
 */
export {
  hoc,
  baseView,
  config,
}

